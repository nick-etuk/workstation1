{
  "dependencies": [],
  "runOnce": true,
  "os": ["win"],
  "checks": { 
    "win": [ 
      "IsDistroInstalled ubuntu",
      "wsl -u root cat /etc/passwd | Select-String -pattern $WSL_USER",
      "wsl -u $WSL_USER ls ~/repos | Select-String -pattern 'nhsapp'"
    ]
  },
  "steps": [
    "install_wsl",
    "check_for_os_updates",
    "install_common_packages",
    "add_wsl_users",
    "sudo_no_password",
    "get_git_repos_wsl"
  ]
}
