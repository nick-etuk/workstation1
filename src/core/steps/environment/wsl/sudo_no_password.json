{
  "dependencies": ["add_wsl_users"],
  "os": ["win"],
  "checks": { 
    "win": [ 
      "wsl -u root test -f /etc/sudoers.d/90-sudo-nopasswd",
      "wsl -u root cat /etc/sudoers.d/90-sudo-nopasswd | Select-String -pattern $WSL_USER"
     ]
  }
}
