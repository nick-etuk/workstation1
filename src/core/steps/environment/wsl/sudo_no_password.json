{
  "dependencies": ["add_WS_USER_UNIXs"],
  "os": ["win"],
  "checks": { 
    "win": [ 
      "wsl -u root test -f /etc/sudoers.d/90-sudo-nopasswd",
      "wsl -u root cat /etc/sudoers.d/90-sudo-nopasswd | Select-String -pattern $WS_USER_UNIX"
     ]
  }
}
